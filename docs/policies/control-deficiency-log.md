# Control deficiency log

**Version:** 1.0 · **Effective date:** {{DATE}} · **Owner:** {{NAME}}

## Purpose

SOC 2 CC4 (monitoring activities) expects evidence that we identify control deficiencies and remediate them. This log is the running record. Auditors will ask to see it. An empty log is a red flag — it means we either have perfect controls (we don't) or we aren't looking.

A "control deficiency" is anything that should have prevented an event but didn't, or any control whose design or operation is known to be weak. It is distinct from:
- **Incident** — a security or availability event (lives in `docs/incidents/`)
- **Risk** — a hypothetical future event (lives in `risk-register.md`)
- **Deficiency** — a known weakness in a control that exists today

A deficiency can be discovered through: failed CI checks, code review, penetration tests, customer reports, internal audits, or a real incident that exposed it.

## How to log a deficiency

Add a new row to the table below. Assign yourself if you found it. Use a short, scannable title. Cite the file or process that's weak. Include enough detail that a stranger 6 months from now can understand what was broken.

## Severity rubric

| Severity | Definition | Example |
|---|---|---|
| **Critical** | Active customer-data exposure, broken access control, audit trail gap | Auth bypass; missing audit row for privileged action |
| **High** | Could result in data exposure or audit failure under common conditions | No backup restore tested; missing CI gate on security branch |
| **Medium** | Hardening gap, won't fail audit alone but compounds risk | Headers missing CSP; npm dep not pinned |
| **Low** | Cosmetic / non-blocking | Doc inconsistency; un-renamed legacy enum |

## Deficiency log

| # | Date opened | Severity | Title | Source | Description | Remediation plan | Owner | Status | Date closed |
|---|---|---|---|---|---|---|---|---|---|
| 1 | 2026-05-25 | Critical | Auth uses dev cookie stub, not real auth | SOC2_READINESS.md gap analysis | `src/lib/auth/session.ts` uses HMAC-signed cookie keyed by `AUTH_STUB_SECRET` and a user-controlled email. Anyone with the secret can impersonate any user. | Swap to Clerk per `auth-swap.md`. Tracked Phase 1 of SOC2_ROADMAP. | {{NAME}} | Open | — |
| 2 | 2026-05-25 | High | No CSP header | SOC2_READINESS.md gap analysis | `next.config.js` ships HSTS + X-Frame-Options + X-Content-Type-Options + Referrer-Policy but no Content-Security-Policy. Next.js inline scripts make naïve CSP break the app. | Implement nonce-based CSP via middleware. Phase 2 of SOC2_ROADMAP. | {{NAME}} | Open | — |
| 3 | 2026-05-25 | High | No backup restore drill | SOC2_READINESS.md gap analysis | We have backups documented in `business-continuity.md` but have never validated a restore. Backup-without-test is "we have hope" not "we have backups." | Quarterly DR drill starting Q3 2026. | {{NAME}} | Open | — |
| 4 | 2026-05-25 | High | npm deps not pinned to exact versions | SOC2_READINESS.md gap analysis | `package.json` uses `^` and `~` ranges. A malicious dep update could ship into prod through Dependabot or fresh install. | Fixed: all 23 ranged deps pinned to the exact versions currently resolved by the lockfile (resolution unchanged — pins taken from installed packages, not latest). Dependabot surfaces upgrades as explicit PRs we review. | {{NAME}} | Closed | 2026-06-12 |
| 5 | 2026-05-25 | Medium | No Sentry / no error tracking | SOC2_READINESS.md gap analysis | Errors live only in Vercel function logs (7-day retention on free tier). Slow incident detection. | Wire Sentry with PII scrubbing. Phase 2. | {{NAME}} | Open | — |
| 6 | 2026-05-25 | Medium | No MFA on Vercel / GitHub / Neon | SOC2_READINESS.md gap analysis | Founder account credential theft = full production compromise. MFA is the single best control here. | Enable MFA on all three; print recovery codes; store physically. Phase 1. | {{NAME}} | Open | — |
| 7 | 2026-05-25 | Medium | No formal access review | SOC2_READINESS.md gap analysis | Solo posture today, but no documented quarterly review. When contributors join this gap becomes Critical. | Add to calendar: quarterly access review. Document in `access-control.md` what gets reviewed. | {{NAME}} | Open | — |
| 8 | 2026-05-25 | Medium | No vendor SOC 2 reports on file | SOC2_READINESS.md gap analysis | We claim Neon, Vercel, Anthropic SOC 2 in `vendor-management.md` but don't have copies. Auditors want to see them. | Request SOC 2 Type 2 from each vendor; file in `docs/vendor-receipts/`. Phase 1. | {{NAME}} | Open | — |
| 9 | 2026-05-25 | Medium | Audit log not replicated outside primary DB | SOC2_READINESS.md gap analysis | If the Neon DB is lost, the audit trail goes with it. Audit logs need to survive DB loss to be credible evidence. | Mirror audit_log to append-only S3 or dedicated DB. Phase 3. | {{NAME}} | Open | — |
| 10 | 2026-05-25 | Low | No formal training / acknowledgement records | SOC2_READINESS.md gap analysis | Policies in `docs/policies/` don't have signed acknowledgement from the founder. SOC 2 expects sign-off evidence per person per year. | Add `docs/policies/acknowledgements/{name}-{year}.md` with content + signature. Annual cadence. | {{NAME}} | Open | — |
| 11 | 2026-05-25 | High | tenantId is nullable; not yet enforced at query layer | Phase 1 of multi-tenancy work | Schema had `tenantId String?` on every tenant-scoped model. Phase 4a wired write enforcement (engine refuses cross-tenant); Phase 4c scoped the highest-leverage read queries; Phase 4b applied `ALTER COLUMN tenantId SET NOT NULL` on 26 tables (audit_log intentionally excluded for pre-identity TOKEN_REJECTED events). Remaining gap: composite `[tenantId, code]` uniques deferred until customer #2 onboards. | Closed via Phases 4a + 4b + 4c. Composite uniques tracked as Phase 4b-followup. | {{NAME}} | Closed | 2026-05-26 |
| 12 | 2026-05-25 | High | No Postgres Row-Level Security (RLS) | Phase 1 of multi-tenancy work | RLS would catch any query that forgets `where: { tenantId }` at the database layer. Application-level scoping is the only enforcement in v1. Documented as a planned follow-up in `docs/multi-tenancy.md`. ⚠️ **2026-08-08 — the Phase 1 policy set was INCOMPLETE and nothing counted it.** Six tenant-scoped tables (`balance_assertion`, `commodity`, `commodity_price`, `lot`, `period_reopen_log`, `report_template`) reached the schema with RLS never enabled and no policy — confirmed both in the DDL and against the live dev database (`relrowsecurity = false`, 0 policies on all six). No live exposure, because Phase 1 FORCEs nothing; the risk was entirely that Phase 3 would report success while leaving those six unprotected — **partial enforcement that reads as complete**. Migration 0042 predicted exactly this in a comment. Policies added, and `tests/rls-policy-coverage.test.ts` now DERIVES the tenant-scoped table set from the schema and fails on the next omission, so the hand-written list in the policy file cannot silently drift again. | Add per-table RLS policies; set `app.current_tenant_id` per Prisma connection. Tracked v2 of multi-tenancy. | {{NAME}} | Open | — |
| 13 | 2026-06-10 | Critical | Migration-only DDL had no reproducible source; production ran without it | 2026-06-10 db:reset incident post-audit | The schema is `db push`-managed; append-only triggers, CHECK constraints, GIN indexes, and the lineage partial unique index existed only in migration SQL — or nowhere (the audit_log trigger and lineage index had no migration at all). The 2026-06-10 reset recovery restored schema + data but none of this DDL, so production ran with no audit-trail no-rewrite enforcement until a live-DB pg_catalog audit caught it. Docs claimed the enforcement existed; the database disagreed. | `prisma/sql/migration-mirror.sql` created as the idempotent single source of truth; applied by `npm run db:restore-ddl`, chained into `db:reset`, applied in CI, re-applied to production and verified via pg_catalog ([PR #232](https://github.com/ledger-nexus/ledger-core/pull/232)). The two objects that had NO migration were first reconstructed from code commentary; their byte-exact pre-incident definitions were then recovered from a Neon point-in-time branch (2026-06-10T21:00:00Z, inside the 6h retention window) and committed as migration `0015_audit_log_rules_and_lineage_uniq` ([PR #234](https://github.com/ledger-nexus/ledger-core/pull/234)). Both reconstructions were materially wrong and repaired: the audit_log mechanism was RULEs not a trigger (see #14), and the lineage index was tenant/book-scoped — `("tenantId","bookId",triple)` — where the reconstruction was a bare triple that would fail the SECOND tenant importing any given ERP record id (CC6.1 cross-tenant import failure + existence leak). Rule: all future migration-only DDL goes in a numbered migration AND the applied-form files. | {{NAME}} | Closed | 2026-06-10 |
| 14 | 2026-06-10 | High | audit_log mutation blocking lost its mechanism after the reset | PR #232 CI run (a loud DELETE trigger failed 21 tests) | First restoration attempt used a RAISE-EXCEPTION trigger; CI failures across 12 test files revealed the original mechanism was different: silent Postgres RULEs (`DO INSTEAD NOTHING` on UPDATE + DELETE), authored in the PR #10 arc (commit a0caf92) with a `withAuditLogMutable` test-only escape hatch and probe tests — extracted but never merged, so the enforcement existed only on the live DB and died with the reset. | Adopted a0caf92 wholesale: `prisma/sql/audit-log-append-only.sql` (applied by `db:restore-ddl` alongside the mirror file, in CI, and re-applied to production), `tests/_helpers/audit-log-cleanup.ts`, probe tests, 17 test files moved to sanctioned cleanup. Both UPDATE and DELETE are blocked again. Known sharp edge: RULEs structurally rewrite Postgres's internal referential-integrity action queries — hard-deleting ANY `app_user` row errors with XX000 while the rules are armed (the `actorUserId` FK's ON DELETE SET NULL action gets rewritten even when zero audit rows reference the user); such deletes must run inside the `withAuditLogMutable` window. Runtime user offboarding is soft-delete (`deactivatedAt`), so this only bites tests and manual surgery. | {{NAME}} | Closed | 2026-06-10 |
| 15 | 2026-06-11 | High | Consolidation report lookups not tenant-scoped (cross-tenant metadata bleed + client-controlled root) | Code audit of `getConsolidatedTrialBalance` (Phase 4c follow-through) | Three unscoped lookups in the consolidation path. (a) The account-metadata `findMany` (subtype/isContra) matched by code alone — a same-code account in ANOTHER tenant carrying an IC subtype (`DUE_FROM_AFFILIATE` etc.) could classify a real balance as an intercompany elimination and zero it from the consolidated TB; `isContra: true` would flip the displayed sign. (b) The root-entity `findFirst` matched by code alone, and BOTH UI callers pass `?root=` from the client — a signed-in user naming another tenant's entity code could consolidate (page) or download (CSV) that tenant's books (CC6.1/CC6.3). (c) `getTrialBalance`'s account scan (`OR: [{entityId: null}, {entityId}]`) had no tenant filter — every tenant's shared chart landed in the scan, leaking other tenants' account codes/names as zero rows and letting a same-code shared account shadow the real one via the by-code dedup (proven by the regression test). | All three fixed in one PR: metadata + TB account scans pinned to `root.tenantId` / the entity's tenant; root lookup takes a `tenantId` pin which the page + CSV route pass from the session (`getCurrentTenant()`), failing closed (404-style throw / 403) on cross-tenant codes. Regression tests: poisoned same-code IC account in a second tenant must not eliminate tenant 1's balance (`tests/consolidation.test.ts`); tenantId-pinned root lookup rejects another tenant's entity code (`tests/tenant-isolation.test.ts`).  Evidence: [PR #237](https://github.com/ledger-nexus/ledger-core/pull/237). | {{NAME}} | Closed | 2026-06-11 |
| 16 | 2026-06-11 | Medium | Remaining report account scans not tenant-scoped (IS, BS, cash flow, M-3) | Sweep triggered by #15 | The same unscoped-account-scan pattern as #15(c) exists in `getIncomeStatement` + `getBalanceSheet` (`src/lib/accounting/reports.ts`), `getCashFlowStatement` (`reports/cash-flow.ts`), and the M-3 detail by-code metadata lookup (`reports/m3-detail.ts`); `book-tax-difference.ts` needs review. Amounts don't leak (line sums are filtered per entity/book) but other tenants' shared-chart account codes/names appear as zero rows, and a same-code shared account in another tenant can shadow the real account's name/type via the by-code dedup. Lower severity than #15: no elimination/classification logic rides on these scans and the root entity is session-derived on these pages. | Fixed in follow-up PR. IS + BS scans pinned via `resolveEntityBook`'s `entityTenantId`, mirroring the `getTrialBalance` fix. `getCashFlowStatement`, `getBookTaxDifference`, and `getM3Detail` now resolve the entity (optional session-`tenantId` pin, threaded from all UI/CSV callers) and scope their account scans to its tenant. The `book-tax-difference.ts` review found it WORSE than logged: its subtype scan read the ENTIRE account table — every tenant — into a last-write-wins by-code map, so another tenant's same-code account could flip a delta's permanent/temporary call (and the M-3 bucket downstream). Five poisoned-shared-account regression tests (`tests/report-tenant-scoping.test.ts`), all verified to fail pre-fix. Sweep of `src/lib/accounting/reports/` confirms no remaining unscoped account/entity lookups.  Evidence: [PR #238](https://github.com/ledger-nexus/ledger-core/pull/238). | {{NAME}} | Closed | 2026-06-11 |
| 28 | 2026-06-12 | High | Cross-tenant write via unscoped `createFixedAsset` entity lookup | 15th adversarial pass (historical finding; pre-dates the Phase 2b sweep) | `createFixedAsset` (`src/lib/accounting/sub-ledgers/fixed-assets.ts`) resolved its legal entity with `findFirstOrThrow` on `code` alone — no tenant scope. A tenant-A bearer-token holder calling `/api/internal/fixed-asset` could create a FixedAsset under tenant-B's entity whenever both tenants used the same entity code — a cross-tenant WRITE (CC6.1). | `CreateFixedAssetInput` gains a required `tenantId`; the entity lookup scopes by `{ code, tenantId }` so cross-tenant code collisions fail closed ("not found"). Call sites updated: the internal fixed-asset route passes `identity.tenantId` from the bearer token; seeds + tests pass their fixture tenant. Verified: `npx tsc --noEmit` clean; 29/29 across `tests/sub-ledgers.test.ts`, `tests/tenant-write-enforcement.test.ts`, `tests/v0-4-features.test.ts`. Evidence: [PR #88](https://github.com/ledger-nexus/ledger-core/pull/88). ⚠️ **Scope of this remediation was narrower than the finding: see #32.** `createFixedAsset` was the only call path fixed; `openApItem` / `openArItem` / `createLease` / `createRevenueContract` carried the identical unscoped lookup until 2026-08-08. | {{NAME}} | Closed | 2026-06-12 |
| 29 | 2026-07-17 | Medium | Access-control docs described an unbuilt RBAC (VIEWER role + `requirePermission` policy layer) | Codex external code review of `main@0cb47d4` (architectural commentary) | `access-control.md` v2.0 and the customer-facing `security-questionnaire-answers.md` described a four-role hierarchy including a read-only `VIEWER`, "16 named permissions" in `src/lib/auth/policy.ts`, and a `requirePermission(...)` call on every Server Action — **none of which exist in code** (no `policy.ts`, no `requirePermission`, `TenantRole` is three roles). It also cited `assertTenantScope()` as the isolation control: that helper *does* exist in `@/lib/soc2` but has **zero call sites**, so the live control is the per-query `WHERE tenantId`, not a post-fetch assertion (wiring it in or deleting it is a follow-up). Implemented reality: three roles (`OWNER`/`ADMIN`/`MEMBER`); enforcement is per-action (`requireCurrentUser()` + `requireCurrentScope()` then `isAdmin()`/`isTenantAdmin()`/`requireAdmin()`); cross-tenant isolation is a per-query `WHERE tenantId` from `getCurrentScope()`. The doc also cited admin routes that don't exist (`/admin/team`, `/admin/reset`, `/admin/tenant`, `/admin/access-review`). A documentation-integrity risk: an auditor asking "show me `policy.ts`" finds nothing, contradicting the doc's own "every row points at a file path" contract. NOT an active access-control exploit — the inline role checks + `tenantId` scoping are a valid working control (see #15/#16 and the 2026-07-17 getScope sweep). | **Docs corrected to match implementation** (access-control.md → v2.1; questionnaire row rewritten): three roles documented, the real enforcement model spelled out, and `VIEWER` + a centralized `requirePermission()` catalog marked **Planned, not implemented**. **Code**: building the centralized permission layer + `VIEWER` tier is deferred as a product decision (owner's call — seat/billing implications); the current per-action enforcement stands until then. `src/lib/auth/tenant.ts` already carries the "when RBAC lands, `isAdmin` retires" note. Evidence: PR (docs-only). | {{NAME}} | Remediated | — |
| 30 | 2026-06-09 | Medium | Legacy reports leak Account metadata across tenants | Report Builder PR 5 surfaced + PR 7 closed (logged as #13 in the PR branch; renumbered on merge — #13 was taken by the 2026-06-10 reset incident) | `getTrialBalance`, `getIncomeStatement`, `getBalanceSheet`, `getCashFlowStatement`, `getBookTaxDifference`, `getM3Detail`, and `getConsolidatedTrialBalance` called `Account.findMany` without a `tenantId` filter. In a multi-tenant DB where two tenants both had a shared `code: "1000"` chart row, dedup arbitrarily picked one tenant's metadata for the other's report — name, parentCode, subtype, isContra bled across tenants. Balances were correct (journal lines are entity-scoped); metadata wasn't. Risk: SOC 2 CC6.1 confidentiality (low-impact in practice — chart vocabulary isn't typically sensitive — but still a confidentiality boundary violation). | Threaded `tenantId` through `resolveEntityBook` return value + Account queries across all 7 sites. PR #201 commit `8fb3c38`. **Same gap independently re-found and fixed on main as #15/#16 (PRs #237/#238) while #201 sat unmerged; main's fix is the one in force — #201's regression test `tests/report-builder-tenant-isolation.test.ts` lands as additional coverage.** Regression test in `tests/report-builder-tenant-isolation.test.ts` proves the gap is closed (two-tenant fixture, asserts each report returns own-tenant metadata). | {{NAME}} | Closed | 2026-06-09 |
| 31 | 2026-06-09 | High | Lost-update race in `updateReportTemplateDefinition` Server Action | Report Builder PR 10 surfaced + PR 11 closed, 35th adversarial-pass (logged as #14 in the PR branch; renumbered on merge) | Two operators editing the same ReportTemplate in parallel: A saves first → row N+1, B saves second → silently overwrites A. Original implementation computed `version: existing.version + 1` from a stale `findFirst`. Without `expectedVersion` plumbing, the second writer wins and there's no audit trail of which edit "won." Real multi-user data-loss risk under the CC6.3 "edit what you saw" invariant. | Defense-in-depth two-layer fix: (1) Server Action requires `expectedVersion` from caller + compares to `existing.version`; (2) Prisma switched from `update({where:{id}})` to `updateMany({where:{id, version: expectedVersion}})` so a true race interleaving the application check still gets caught at the DB layer (updated.count === 0). PR #201 commit `19c5da4`. 4-test regression in `tests/report-builder-concurrency.test.ts` covers matching-version success, stale-version rejection, fresh-version success, and the simulated true-race where an external writer slips in between findFirst and updateMany inside a transaction. | {{NAME}} | Closed | 2026-06-09 |
| 32 | 2026-08-08 | High | Deficiency #28's remediation covered 1 of 5 sub-ledger write paths | Follow-up sweep after the mapper tenant-scoping work (PRs #370/#371); found by asking whether an `exportToNs` leak was one file or the pattern | #28 ("Cross-tenant write via unscoped `createFixedAsset` entity lookup") was recorded **Closed** on 2026-06-12. Its fix — a required `tenantId` on the input and a `{ code, tenantId }` entity lookup — was applied to `createFixedAsset` **only**. The identical `findFirstOrThrow({ where: { code } })` remained in `openApItem`, `openArItem`, `createLease` and `createRevenueContract` for two months, under comments that named the hazard and deferred it (`ar.ts`: "Single-tenant world today... Multi-tenant callers should ensure they only pass codes they own"). Same CC6.1 exposure as #28, in live accounting code: a caller naming an entity code another tenant also uses writes into that tenant's books. **Demonstrated**, not inferred — with the four lookups reverted, all four calls with `tenantId=B, entityCode=A's` returned `promise resolved instead of rejecting` and left rows behind. | All four inputs gain a required `tenantId`; lookups scope by `{ tenantId, code }`, so a cross-tenant code fails closed as "not found" without disclosing that another tenant owns it. 42 call sites updated across seeds, both ERP mappers and 12 test suites — `tenantId` is required, not optional, so `tsc` enumerates every missed caller rather than leaving silent gaps. New `describe` block in `tests/tenant-write-enforcement.test.ts` covers all four refusals, each seen to fail against the reverted lookup. `tests/tenant-scope-guard.test.ts` baseline 56 → 52. ⚠️ **Root cause of the two-month gap: nothing enforced the convention** — #28 was closed by fixing the one function that was reported, with no check that its siblings shared the defect. That guard now exists (PR #371). | {{NAME}} | Closed | 2026-08-08 |

## Procedure

1. **Discover**: Found a control gap? Add a row. Don't hide it.
2. **Triage**: Assign severity per rubric above. If Critical or High, also open a risk-register row if not already there.
3. **Remediate**: Track the remediation plan in the row. Update Status as work progresses (Open → In Progress → Remediated).
4. **Verify**: When remediated, run the test that proves the gap is closed. Note the test in the remediation plan.
5. **Close**: Move Status to Closed. Fill in Date closed. Do NOT delete the row — auditors want history.

## Statuses

- **Open** — identified, not yet under active remediation
- **In Progress** — actively being worked
- **Remediated** — code/process change made, not yet verified by retest
- **Closed** — verified, gap is no longer present
- **Accepted Risk** — leadership decided not to fix (rare; requires explicit justification in the row)

## Annual review

Reviewed annually on {{REVIEW_DATE}}. Walk through each Open + In Progress + Remediated row and confirm status is current. Closed and Accepted Risk rows stay in the log as historical evidence — never deleted.
