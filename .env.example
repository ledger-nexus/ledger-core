# Local development: use Neon's free tier (https://neon.tech) or a local Postgres.
# Format: postgresql://user:password@host:port/dbname
DATABASE_URL="postgresql://postgres:postgres@localhost:5432/mini_ledger?schema=public"

# Only needed for the bank-recon and rev-rec projects; mini-ledger does not use AI.
# ANTHROPIC_API_KEY=""
